<UserControl x:Class="BalatroSaveExplorer.Controls.Tabs.FileInfoTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  Margin="5">
        <StackPanel Margin="10">
            <GroupBox Header="File Information"
                      Margin="0,0,0,15">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"
                                          MinWidth="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0"
                           Grid.Column="0"
                           Content="File Path:"
                           FontWeight="Bold"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                    <TextBox Grid.Row="0"
                             Grid.Column="1"
                             x:Name="FilePathTextBox"
                             MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid},
                                        Converter={StaticResource PercentConverter}, ConverterParameter=0.7}"
                             IsReadOnly="True"
                             VerticalAlignment="Center"
                             Margin="0,0,0,5"/>

                    <Label Grid.Row="1"
                           Grid.Column="0"
                           Content="File Size:"
                           FontWeight="Bold"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                    <TextBox Grid.Row="1"
                             Grid.Column="1"
                             x:Name="FileSizeTextBox"
                             MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid},
                                        Converter={StaticResource PercentConverter}, ConverterParameter=0.7}"
                             IsReadOnly="True"
                             VerticalAlignment="Center"
                             Margin="0,0,0,5"/>

                    <Label Grid.Row="2"
                           Grid.Column="0"
                           Content="Last Modified:"
                           FontWeight="Bold"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                    <TextBox Grid.Row="2"
                             Grid.Column="1"
                             x:Name="LastModifiedTextBox"
                             MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid},
                                        Converter={StaticResource PercentConverter}, ConverterParameter=0.7}"
                             IsReadOnly="True"
                             VerticalAlignment="Center"
                             Margin="0,0,0,5"/>

                    <Label Grid.Row="3"
                           Grid.Column="0"
                           Content="Compression:"
                           FontWeight="Bold"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                    <TextBox Grid.Row="3"
                             Grid.Column="1"
                             x:Name="CompressionInfoTextBox"
                             MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid},
                                        Converter={StaticResource PercentConverter}, ConverterParameter=0.7}"
                             IsReadOnly="True"
                             VerticalAlignment="Center"
                             Margin="0,0,0,5"/>

                    <Label Grid.Row="4"
                           Grid.Column="0"
                           Content="Content Type:"
                           FontWeight="Bold"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                    <TextBox Grid.Row="4"
                             Grid.Column="1"
                             x:Name="ContentTypeTextBox"
                             MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid},
                                        Converter={StaticResource PercentConverter}, ConverterParameter=0.7}"
                             IsReadOnly="True"
                             VerticalAlignment="Center"
                             Margin="0,0,0,5"/>

                    <Label Grid.Row="5"
                           Grid.Column="0"
                           Content="Entries Count:"
                           FontWeight="Bold"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                    <TextBox Grid.Row="5"
                             Grid.Column="1"
                             x:Name="EntriesCountTextBox"
                             MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid},
                                        Converter={StaticResource PercentConverter}, ConverterParameter=0.7}"
                             IsReadOnly="True"
                             VerticalAlignment="Center"
                             Margin="0,0,0,5"/>
                </Grid>
            </GroupBox>

            <GroupBox Header="Raw Content"
                      Margin="0,0,0,15">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto"
                              Margin="5"
                              Height="300">
                    <TextBox x:Name="RawContentTextBox"
                             IsReadOnly="True"
                             TextWrapping="NoWrap"
                             FontFamily="Consolas"
                             FontSize="12"
                             Background="{DynamicResource TextBoxBackgroundBrush}"
                             Foreground="{DynamicResource PrimaryTextBrush}"
                             BorderThickness="0"/>
                </ScrollViewer>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
