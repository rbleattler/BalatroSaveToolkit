<UserControl x:Class="BalatroSaveExplorer.Controls.SearchableTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BalatroSaveExplorer.Controls"
             xmlns:converters="clr-namespace:BalatroSaveExplorer.Converters">

    <UserControl.Resources>
        <converters:StringNullOrEmptyToBoolConverter x:Key="StringNullOrEmptyToBoolConverter"/>
    </UserControl.Resources>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search Box -->
        <Border Grid.Row="0"
                Background="{DynamicResource InputBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Margin="0,0,0,10"
                MinHeight="32">
            <Grid Margin="4,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Icon -->
                <Path Grid.Column="0"
                      Margin="8,0,4,0"
                      VerticalAlignment="Center"
                      Fill="{DynamicResource SecondaryTextBrush}"
                      Data="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"
                      Width="16"
                      Height="16"/>

                <!-- Search TextBox -->
                <TextBox x:Name="SearchTextBox"
                         Grid.Column="1"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="{DynamicResource PrimaryTextBrush}"
                         FontSize="{DynamicResource StandardFontSize}"
                         Padding="4,8"
                         VerticalAlignment="Center"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Search Placeholder -->
                <TextBlock Grid.Column="1"
                           Text="🔍 Search tree..."
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontSize="{DynamicResource StandardFontSize}"
                           Padding="4,8"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility"
                                    Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchTextBox}"
                                             Value="">
                                    <Setter Property="Visibility"
                                            Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Clear Button -->
                <Button Grid.Column="2"
                        x:Name="ClearButton"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="8"
                        Command="{Binding ClearSearchCommand}"
                        ToolTip="Clear search">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Visibility"
                                    Value="Collapsed"/>
                            <Setter Property="Cursor"
                                    Value="Hand"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchTextBox}"
                                             Value="">
                                    <Setter Property="Visibility"
                                            Value="Collapsed"/>
                                </DataTrigger>
                                <Trigger Property="DataContext"
                                         Value="{x:Null}">
                                    <Setter Property="Visibility"
                                            Value="Collapsed"/>
                                </Trigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Text, ElementName=SearchTextBox, Converter={StaticResource StringNullOrEmptyToBoolConverter}}"
                                                   Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility"
                                            Value="Visible"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Path Fill="{DynamicResource SecondaryTextBrush}"
                          Data="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"
                          Width="12"
                          Height="12"/>
                </Button>
            </Grid>
        </Border>

        <!-- TreeView -->
        <TreeView Grid.Row="1"
                  x:Name="FilteredTreeView"
                  ItemsSource="{Binding FilteredItems}"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding DisplayName}"
                                   Foreground="{DynamicResource PrimaryTextBrush}"
                                   FontWeight="SemiBold"/>
                        <TextBlock Text=": "
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   Margin="2,0"/>
                        <TextBlock Text="{Binding ValueDisplay}"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   FontStyle="Italic"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <!-- No Results Message -->
        <Border Grid.Row="1"
                Background="{DynamicResource BackgroundBrush}"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility"
                            Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasResults}"
                                     Value="False">
                            <Setter Property="Visibility"
                                    Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel Orientation="Vertical"
                        HorizontalAlignment="Center"
                        Margin="20">
                <TextBlock Text="🔍"
                           FontSize="24"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                <TextBlock Text="No results found"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontSize="{DynamicResource StandardFontSize}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,4"/>
                <TextBlock Text="{Binding SearchText, StringFormat='for &quot;{0}&quot;'}"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontSize="{DynamicResource SmallFontSize}"
                           HorizontalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility"
                                    Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SearchText, Converter={StaticResource StringNullOrEmptyToBoolConverter}}"
                                             Value="False">
                                    <Setter Property="Visibility"
                                            Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
