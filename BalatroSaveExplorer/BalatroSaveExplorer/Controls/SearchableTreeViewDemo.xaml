<Window x:Class="BalatroSaveExplorer.Controls.SearchableTreeViewDemo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:BalatroSaveExplorer.Controls"
        Title="Searchable TreeView Demo"
        Height="600"
        Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0"
                Margin="0,0,0,20">
            <TextBlock Text="🔍 Searchable TreeView Demo"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
            <TextBlock Text="Try searching for: joker, tarot, spades, mercury, boss, etc."
                       FontSize="14"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       HorizontalAlignment="Center"
                       FontStyle="Italic"/>
        </StackPanel>

        <!-- SearchableTreeView -->
        <Border Grid.Row="1"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Background="{DynamicResource BackgroundBrush}">
            <controls:SearchableTreeView
                ItemsSource="{Binding SampleData}"
                Margin="10"/>
        </Border>
    </Grid>
</Window>
